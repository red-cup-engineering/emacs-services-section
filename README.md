# Emacs services section

The public boundary for Red Cup Engineering's Emacs-facing interface work.

`lib/legacy` is intentionally a private submodule because it contains
preserved user configuration, historical source material, and network captures.
Authorized maintainers can initialize it with:

```sh
git submodule update --init --recursive
```
